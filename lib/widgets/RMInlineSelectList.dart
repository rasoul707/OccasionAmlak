import 'package:flutter/material.dart';
import '../data/colors.dart';

class RMInlineSelectList extends StatelessWidget {
  RMInlineSelectList({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
